import { makeAction } from '../../utils';

export const startLive = makeAction('widget.exchange.live-wallet-rates.flow.start-live');
export const stopLive = makeAction('widget.exchange.live-wallet-rates.flow.stop-live');
